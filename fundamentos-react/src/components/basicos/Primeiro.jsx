import React from 'react'

export default _ => {
    
    const msg = 'Seja Bem-vindo(a)'

    return (
        <div>
            <h2>Primeiro Componente</h2>
            <p>{ msg }</p>
        </div>
    )

}